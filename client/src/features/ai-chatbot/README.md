# AI Chatbot (this branch)

- **UI:** `pages/AIChatbotPage.jsx` + `../styles/chatbot.css`
- **API stub:** `services/chatApi.js` — replace with `fetch`/`axios` to your RAG route after merging `main`.

When you merge into `main`, mount this page at the route your student dashboard button uses (e.g. `/student-dashboard/ai-chatbot`) and implement the backend under `server/rag/` + `server/services/ai/`.
